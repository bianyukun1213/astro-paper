---
import IconUser from "@/assets/icons/IconUser.svg";
import { useTranslations, type Locale } from "@/utils/locale";
import type { HCard as HCardType } from "@/utils/types";
import HCard from "./HCard.astro";

const currentLocale = Astro.currentLocale as Locale;
const m = useTranslations(currentLocale);

type Props = {
  author: string;
  hCard: HCardType;
  class?: string;
};

const { author, hCard, class: className = "" } = Astro.props;
---

<div
  class:list={["flex items-center gap-x-2 opacity-80", className]}
  title={m.text_author()}
>
  <IconUser class:list={["inline-block size-6 min-w-5.5"]} />
  <span>{author}</span>
  <HCard {hCard} />
</div>
